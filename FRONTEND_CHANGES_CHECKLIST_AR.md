# تقرير تغييرات الواجهة الأمامية — السجل الكامل (من البداية حتى اليوم)

> هذا الملف يوثق جميع تغييرات الواجهة الأمامية منذ بداية المشروع وحتى الآن، مع تنظيم مختصر وواضح.

---

## 1) الانتقال إلى Next.js (التحديث الأساسي)
### لماذا هذا التحول؟
| الميزة | التأثير |
| :--- | :--- |
| تحسين SEO | صفحات قابلة للأرشفة بدل SPA فارغة |
| تحسين الصور | `next/image` يقلل الحجم ويمنع CLS |
| App Router | مسارات حديثة + Layouts + Partial Rendering |

### قبل/بعد
**قبل (Vite SPA):**
- توجيه عبر `wouter` بالكامل في المتصفح.
- صور بـ `<img>` بدون تحسين.
- بناء عبر `vite.config.ts` و `index.html`.

**بعد (Next.js App Router):**
- توجيه Hybrid عبر `app/`.
- صور عبر `<Image />`.
- بناء موحد في `next.config.js` مع SSR/SSG.

### تحسينات تقنية رئيسية
- استبدال `wouter` بـ Next.js routing بالكامل.
- إضافة `sitemap.ts`, `robots.ts` و `Metadata` في `layout.tsx`.
- حذف ملفات Vite القديمة وتحديث `package.json`.
- إزالة `recharts` غير المستخدمة (تحليل Bundle Analyzer).

---

## 2) QA & Post-Migration Polish
### إصلاحات بناء وأنواع
- إصلاح أنواع `chat.tsx` (ChatData, getAvatarColor).
- تصحيح Props في `AdminLogin`.
- معالجة استيراد `@shared/schema` في `calendar.tsx`.

### استراتيجية العرض
- صفحات SSG: الرئيسية، لوحة التحكم، تسجيل الدخول.
- صفحات SSR: `/chat/[id]` وصفحات الإدارة الديناميكية.

### تحسين الوصول
- إضافة `aria-label` لكل زر أيقوني (Size="icon").

### تهيئة نهائية
- إصلاح تحذيرات `outputFileTracingRoot`.
- الحفاظ على ألوان التصميم مع بناء نظيف.

### نماذج (Formik/Yup)
- إعادة بناء جميع النماذج (تسجيل/تسجيل دخول/إعدادات/بوابة).
- تحقق فوري ورسائل خطأ واضحة.

---

## 3) System Integration & Routing Fixes (14 Feb Update 2)
- إصلاح توجيه تسجيل الدخول إلى `/dashboard`.
- Proxy لكل `/api/*` إلى `localhost:5000` لتفادي CORS.
- تزويد React Query بـ `defaultQueryFn`.

---

## 4) Phase 3 & 4 (14 Feb Update 3)
### لوحة الإدارة
- إدارة المستخدمين، المالية، المزودين.

### SEO متقدم
- Metadata لكل صفحة.
- JSON-LD.
- Sitemap ديناميكي.

### Performance
- تحويل صفحات الإدارة إلى Server Components.
- `next/dynamic` للأجزاء الثقيلة.

---

## 5) SSR Fixes & Security (15 Feb)
- حل `ssr: false` في Server Components عبر Loaders.
- حماية المسارات (`/admin`, `/provider`) عبر RoleGuard.
- Logout مع reload كامل لتنظيف الحالة.

---

## 6) Mobile Responsiveness (15 Feb Update 2)
- إصلاح Sidebar الشفافة.
- Overflow أفقي في (Finance/Cleaning/Providers).
- Grid responsive في Listing Detail.
- عرض محادثات موبايل بنمط "قائمة/محادثة".

---

## 7) Image Integration (15 Feb Update 3)
- Proxy `/uploads/` عبر Next rewrites.
- تحسين تجربة رفع الصور + التوافق مع Cropper.

---

## 8) Implementation Sessions (16 Feb)
### ملخص اليوم (What / How / Why)
- **What:** تحسين UX التسجيل/الدخول + فرض Email Verification على تجربة المستخدم + Events حقيقية بدل mockups + أيقونات قنوات من Assets + صفحات قانونية واضحة + إصلاحات Admin login routing.
- **How:** تحديث `RoleGuard` + تحسين صفحة `/verify-email` (resend + logout-to-login) + ربط Home بـ `/api/events/upcoming` + تحديث `next/image remotePatterns` + تحديث `channel-icon` لاستخدام `assets/integerations`.
- **Why/Impact:** تجربة أوضح وأقل loops، وواجهة أكثر احترافية، وميزة Events ذات قيمة فعلية للمضيف.

### جودة واستقرار (ملاحظات عامة)
- إصلاح قواعد الـ Hooks داخل صفحة الأتمتة لتفادي أخطاء runtime وعدم اتساق الـ state.
- تحسين تزامن الحالة بين بعض المسارات (مثل `/settings` و`/checkin`) لتقليل “اختلاف الشاشة” بعد التنقل.
- تحسين الـ Header لتقليل الوميض أثناء إعادة التوجيه (Redirect flicker) خصوصاً على الجوال.

### أداء وتصميم (ملاحظات عامة)
- تحسين الأداء باستخدام `useMemo` و`useCallback` و`React.memo` في الأماكن التي كانت تسبب re-renders غير ضرورية.
- توحيد الثيم/الألوان داخل بطاقات الأتمتة (مثل استخدام `text-primary`) لضمان تطابق الهوية البصرية.

### نماذج وتكوينات (ملاحظات عامة)
- تحويل نماذج القوالب وتهيئة القوائم إلى `Formik` لتوحيد إدارة الحالة والتحقق.
- تثبيت منطق “Apply All” لضبط القيم وإعادة التصفير (Reset) بشكل أدق.

### Session 2 — Channels + Calendar
- زر Connect → `/channels`.
- Connected Channels شرطيًا + More دائمًا.
- Redirect `/settings/reminders` → `/settings/automation`.
- All option في Calendar.
- إزالة Upcoming Events من التقويم فقط.

### Session 3 — Unit Details
- صور الوحدة أعلى الصفحة + عداد 8 صور.
- رفع متعدد + تأكيد حذف.
- Connected Channels داخل تفاصيل الوحدة مع Disconnect و DELETE.

### Session 4 — OAuth + Signup/Profile + Cleaning
- OAuth mock + صفحة callback.
- Signup محسن + Terms/Privacy placeholders.
- Currency selector + `formatMoney()`.
- ضغط الصور عند >2MB.
- صور تقارير التنظيف مع شروط After Cleaning.
- Dismiss لتنبيه mapping.
- Contact في Maintenance عبر `tel:`.

### Session 5 — Run-ready
- لا تغييرات Frontend (تم التحقق من `npm run build`).

### Session 6 — Email Verification UX
- إزالة كلمة المرور من Add User (Admin).
- صفحات `/set-password` و`/verify-email`.
- زر إعادة إرسال التحقق في Profile.

### Session 7 — Calendar All + Currency Formatting
- All view بألوان لكل وحدة + disable block/unblock.
- توحيد عرض العملة عبر الشاشات الحرجة.

### Session 8 — Chat Input Alignment
- Client chat: Input ثابت بدل Textarea.
- Provider chat: أسلوب مطابق للأدمن.

### Session 9 — Support Chat Position
- تثبيت مدخل الرسائل أسفل الشاشة.

### Session 10 — Mobile Chat + Provider Layout
- رفع المدخل فوق Bottom Tabs بمسافة بسيطة.
- Provider Portal: Sidebar على Desktop و Bottom Tabs على Mobile.
- توسيع القسم الأيمن (المحتوى/الشات) للعرض الكامل.

### Session 11 — Legal Pages + Auth Polish
- **What:** تحويل الشروط/الخصوصية إلى صفحات + تحسينات صغيرة في UX صفحات التسجيل/الدخول.
- **How:** صفحات `/terms` و`/privacy` داخل `client/app/*` وربطها من صفحات auth بدل modal.
- **Why/Impact:** روابط قابلة للمشاركة وتجربة أوضح للمستخدم.

### Session 12 — Admin Login Routing + Setup UI
- **What:** جعل `/admin/login` و`/admin/admin-login` قابلة للوصول للمستخدم logged-out + إخفاء خيار setup في الإنتاج.
- **How:** استثناء صفحات login من guards + alias route + flags للتحكم في ظهور setup.
- **Why/Impact:** يمنع حبس صفحة تسجيل دخول الادمن خلف guard ويقلّل مخاطر إعداد admin في الإنتاج.

### Session 13 — Channel Icons (Assets)
- **What:** استبدال أيقونات القنوات بالصور الرسمية داخل `assets/integerations`.
- **How:** تحديث `client/components/channel-icon.tsx` لاستخدام `next/image` مع fallback عند عدم توفر asset.
- **Why/Impact:** واجهة أكثر احترافية وتطابق بصري أفضل.

### Session 14 — Upcoming Events (Real Data)
- **What:** عرض أحداث حقيقية في Home بدل mockups + دعم thumbnails.
- **How:** جلب `GET /api/events/upcoming` داخل `client/components/pages/home.tsx` + تحديث `client/next.config.js` للسماح بالدومينات الخارجية + إضافة حقل `Events location` لكل وحدة داخل `client/components/pages/settings-automation.tsx`.
- **Why/Impact:** قيمة حقيقية للمضيف حسب مواقع وحداته، مع عرض صور بدون مشاكل Next Image.

### Session 15 — Email Verification UX (No Loops)
- **What:** منع الدخول للتطبيق بدون توثيق البريد + تحسين صفحة التوثيق (resend فقط + logout-to-login) + إصلاح toast crash.
- **How:** تحديث `client/components/auth/role-guard.tsx` للتحقق من `emailVerified`/`emailVerifiedAt` وتوجيه `/verify-email` + تحسين `client/app/verify-email/page.tsx` + تمرير `altText` في ToastAction بصفحة `signup`.
- **Why/Impact:** يمنع loops بين `/login` و`/verify-email` ويجعل خطوة التوثيق واضحة ومستقرة للمستخدم.

### Session 16 — Automation Copy Alignment
- **What:** تعديل نص “Checkout Thank-you” ليعكس الإرسال قبل checkout بـ 30 دقيقة.
- **How:** تحديث copy داخل `client/components/pages/settings-automation.tsx`.
- **Why/Impact:** توحيد ما يعرضه الـ UI مع سلوك السيرفر لتقليل الالتباس.


---

## Session 17 — Split Repo Docker Deploy (18 Feb 2026)
- **What:** تشغيل الواجهة من ريبـو مستقل ضمن stack واحد (backend compose) على Staging.
- **How:**
  - Dockerfile للواجهة أصبح standalone (Next.js في root) بدل monorepo layout.
  - إضافة/تضمين `shared/` داخل ريبـو الواجهة لحل `@shared/*` أثناء build داخل Docker.
  - تعطيل `npm prune --omit=dev` في runtime image لتفادي فشل `next start` بسبب `@next/bundle-analyzer` في `next.config.js`.
- **Why/Impact:** بناء وتشغيل مستقر داخل Docker على السيرفر بدون تعديلات يدوية بعد pull.

---

## Appendix — Full Sync From frontend_today.md (2026-02-17)

# تقرير عمل الواجهة الأمامية — 16 فبراير 2026

## ✅ الهدف والنتيجة
**الهدف**
- تحسين تجربة التسجيل/تسجيل الدخول مع صفحات قانونية واضحة.
- ضمان أن المستخدم غير الموثّق بالبريد لا يستطيع الدخول للتطبيق (بدون حلقات أو UX مربك).
- استبدال mockups في الأحداث والقنوات ببيانات/Assets حقيقية.

**النتيجة**
- تدفق auth أنظف: رسائل واضحة + صفحة توثيق محسّنة + guards تمنع الدخول بدون verification.
- صفحات `/terms` و`/privacy` جاهزة كرابط عادي (بدون Modal).
- صفحة الـ Home تعرض أحداث حقيقية من الـ API وتعرض thumbnails بشكل صحيح.

---

## 🔐 Auth UX (Signup / Login / Guards)
**What**
- تحديث صفحات التسجيل/تسجيل الدخول وتحسين التجربة على الموبايل.
- تطبيق guard على المسارات المحمية لإجبار المستخدم على صفحة التوثيق قبل دخول التطبيق.

**How**
- تحسين نموذج التسجيل: حقول + validation + تطبيع الإيميل/الهاتف داخل `client/components/pages/signup.tsx`.
- تحسين صفحة تسجيل الدخول وإضافة زر رجوع وتجربة UI أوضح داخل `client/components/pages/login.tsx`.
- `RoleGuard` أصبح يتحقق من `emailVerified`/`emailVerifiedAt` ويوجه إلى `/verify-email` عند الحاجة: `client/components/auth/role-guard.tsx`.

**Why / Impact**
- يقلّل أخطاء التسجيل ويجعل التدفق متوقعاً على الموبايل.
- يمنع “دخول بدون توثيق” ويعطي المستخدم خطوة واضحة لإكمال التسجيل.

---

## ✅ Verify Email Page (Resend + Logout + No Loops)
**What**
- صفحة `/verify-email` تدعم “بدون token” لعرض زر resend.
- زر “Go to login” واحد فقط ويعمل “logout ثم redirect” بدون رجوع تلقائي لصفحة التوثيق.
- زر resend يعيد الإرسال فقط بدون تغيير الـ UI أو إظهار أزرار dev إضافية.

**How**
- تنفيذ logout من الصفحة + تنظيف cache الخاصة بالمستخدم ثم redirect hard إلى `/login` داخل `client/app/verify-email/page.tsx`.
- تبسيط الـ UI: إزالة زر “Open verification link” من resend flow.

**Why / Impact**
- يمنع حلقة: `/login` → redirect → `/dashboard` → guard → `/verify-email`.
- يجعل المستخدم يفهم “أنا محتاج أوثّق” بدون واجهة تتغير بشكل مفاجئ.

---

## 🧑‍💼 Admin Login + First-time Setup (UI)
**What**
- جعل صفحة admin login قابلة للوصول حتى للمستخدم logged-out.
- إظهار زر “Set up admin account” فقط في التطوير أو عند تفعيل flag.

**How**
- استثناء `/admin/login` و`/admin/admin-login` من `RoleGuard` + إضافة alias page.
- إضافة flags: `NEXT_PUBLIC_ADMIN_SETUP_ENABLED` + دعم توكن setup.

**Why / Impact**
- يمنع حبس مسار تسجيل دخول الادمن خلف guard.
- يقلّل خطر ظهور خيارات إعداد admin في الإنتاج بدون قصد.

---

## 📅 Upcoming Events (Real Data)
**What**
- استبدال mockups في قسم الأحداث على Home ببيانات حقيقية من `GET /api/events/upcoming`.

**How**
- ربط Home بالـ API وعرض النتائج مع locations المستخدمة.
- تحديث `client/next.config.js` للسماح بعرض thumbnails من دومينات SerpApi/Google عبر `next/image`.
- إضافة input “Events location” لكل وحدة داخل `client/components/pages/settings-automation.tsx`.

**Why / Impact**
- يعطي قيمة مباشرة للمضيف: أحداث مرتبطة بمواقع وحداته.
- يمنع مشاكل عرض الصور بسبب قيود Next Image.

---

## 🎛️ Automation UI (Copy + Per-unit Config)
**What**
- تحديث نص “Checkout Thank-you” ليعكس الإرسال قبل checkout بـ 30 دقيقة.

**How**
- تعديل copy و`statusText` داخل `client/components/pages/settings-automation.tsx`.

**Why / Impact**
- يقلّل الالتباس بين ما يعرضه الـ UI وما ينفذه السيرفر.

---

## 🧾 Legal Pages
**What**
- تحويل الشروط والخصوصية إلى صفحات مستقلة بدل Modal.

**How**
- إضافة `/terms` و`/privacy` وربطهما من صفحات auth.

**Why / Impact**
- تجربة أوضح للمستخدم وإمكانية مشاركة الرابط بسهولة.

---

## 🖼️ Channel Icons
**What**
- استبدال أيقونات القنوات بالـ assets الفعلية في `client/assets/integerations/*`.

**How**
- استخدام `next/image` داخل `client/components/channel-icon.tsx` مع fallback عند عدم توفر asset.

**Why / Impact**
- واجهة أكثر احترافية وتطابقاً مع الهوية البصرية.

---

## 🧩 Marketplace UI (Providers / Hoster / Admin)
**What**
- إضافة واجهات أولية لبوابة المزودين Marketplace والبحث/المعاينة للمضيفين ولوحة تحكم الشركة (admin).

**How**
- إضافة تبويب Marketplace داخل بوابة المزودين لطلبات السوق والحالات والرفع: `client/components/pages/provider-portal.tsx`.
- صفحة مضيفين جديدة للبحث وإنشاء الطلبات والمراجعات: `client/components/pages/settings-provider-marketplace.tsx` + `client/app/settings/providers-marketplace/page.tsx`.
- لوحة Admin جديدة لإدارة الموظفين والطلبات والموافقات والشركة: `client/components/pages/admin/admin-marketplace.tsx` + `client/app/admin/marketplace/page.tsx`.
- إضافة رابط جديد في إعدادات المضيف + عنصر ملاحة جديد في شريط Admin: `client/components/pages/settings.tsx` و`client/components/pages/admin/admin-layout.tsx`.

**Why / Impact**
- تمكين الواجهة لاستهلاك الـ API الجديد للـ Marketplace دون التأثير على المسارات القديمة.

---

## 🔧 إصلاحات Marketplace UI
**What**
- تحديث الواجهة لتوافق علاقة المراجعة الجديدة (واحدة‑لواحدة).

**How**
- تعديل صفحة marketplace للمضيف لاستخدام `review` بدل `reviews`.

**Why / Impact**
- يزيل أخطاء التوافق بين الواجهة والـ API بعد تحديث Prisma.

---

## 🧪 إصلاحات البناء (Build Fixes)
**What**
- إصلاح أخطاء TypeScript أثناء `next build`.

**How**
- تعديل ref callback في صفحة التحقق ليعود `void` بدل العنصر: `client/app/auth/verify-email/page.tsx`.
- نقل منطق تهيئة نموذج الشركة إلى `useEffect` بدلاً من `onSuccess` في `useQuery`: `client/components/pages/admin/admin-marketplace.tsx`.

**Why / Impact**
- نجاح فحص الأنواع أثناء البناء بدون أخطاء.

---

## 🧩 تحديثات اليوم — نموذج الاشتراكات
**What**
- تحديث واجهة المضيف لعرض الاشتراكات والمهام والتسعير حسب نموذج الاشتراكات.
- إعادة ربط بوابة المزودين بنقاط النهاية الجديدة للمهام والرفع.
- تحديث لوحة Admin لتدعم الطلبات والمهام واللوحة الإحصائية وبيانات الشركة.

**How**
- تعديل `client/components/pages/settings-provider-marketplace.tsx` لاستخدام `subscription-requests` و`tasks` وإظهار الاشتراكات والتسعير.
- تعديل `client/components/pages/provider-portal.tsx` لاستخدام `/api/provider/tasks/*` مع رفع الصور وإرسال الإنهاء.
- تعديل `client/components/pages/admin/admin-marketplace.tsx` لعرض Dashboard + طلبات الاشتراك + مهام التعيين + تحديث الشركة.

**Why / Impact**
- توافق فعلي بين الواجهة والـ API الجديد لنموذج الاشتراكات، مع تدفقات تشغيل أساسية لكل الأدوار.

---

## 🎨 توحيد المودالات والكروت والنماذج (Design Consistency)
**What**
- توحيد أنماط المودالات والكروت وحاويات النماذج لتطابق تصميم admin-login (حدود/ألوان/مساحات/ظلال).

**How**
- تحديث مكونات الـ UI الأساسية (Dialog, AlertDialog, Sheet, Drawer, Popover, HoverCard, Select) لتستخدم `bg-card`, `text-card-foreground`, حدود `border`, زوايا `rounded-xl` وظل `shadow-sm`.
- تحديث `Input` و`Textarea` و`SelectTrigger` لاستخدام `bg-card` كخلفية افتراضية.

**Why / Impact**
- واجهة متسقة بصرياً عبر كل المودالات والنماذج بدون اختلافات بين الصفحات.

---

## 🧹 تنظيف Marketplace للمضيف + إصلاح صيانة/تنظيف
**What**
- إزالة صفحة Marketplace الخاصة بالمضيف من الإعدادات.
- إضافة قسم “طلبات الإنهاء” في صفحة الصيانة ليوافق المضيف على إكمال المهمة.
- إصلاح جلب الوحدات داخل نافذة الاشتراك في خدمات التنظيف.

**How**
- حذف صفحة Marketplace للمضيف وربطها من الإعدادات: `client/app/settings/providers-marketplace/page.tsx` + `client/components/pages/settings-provider-marketplace.tsx`.
- إضافة قائمة “Done Requests” مع زر قبول في `client/components/pages/settings-maintenance.tsx`.
- تحديث جلب الوحدات ليستخدم `/api/listings` مع fallback إلى `/api/settings` عند الحاجة.

**Why / Impact**
- تبسيط الإعدادات للمضيف وإزالة ميزات غير مطلوبة.
- المضيف يستطيع الآن اعتماد إنهاء مهام الصيانة من واجهة واضحة.
- نافذة الاشتراك في التنظيف تعرض الوحدات بشكل صحيح بدل “No listings found”.

---

## 🧾 تحديثات اليوم — UX Portal + الصيانة + مزودي الخدمة
**What**
- ربط واجهة الصيانة بنقاط النهاية الجديدة للاشتراكات وطلبات الخدمة.
- إضافة إلغاء اشتراك (Unsubscribe) من صفحة مزودي الخدمة للمضيف.
- تحسين تسميات عناصر الإعدادات لتطابق المحتوى الحقيقي.
- تحسين بوابة المزود (Provider Portal): تنظيم المحتوى وتقليل الفراغات.
- فصل تاريخ تقارير الزيارات في تبويب مستقل مع عرض صور التقرير القديمة.

**How**
- تحديث `client/components/pages/settings-maintenance.tsx` لاستخدام:
  - `GET /api/maintenance/subscriptions`
  - `POST /api/maintenance/tasks/request`
  مع نموذج طلب خدمة (تاريخ/وقت/عنوان/إحداثيات).
- إضافة زر Unsubscribe في `client/components/pages/settings-cleaning.tsx` وربطه بـ:
  - `DELETE /api/cleaning/subscriptions/:id`
- تحديث نصوص الإعدادات في `client/components/pages/settings.tsx`:
  - `Cleaning Services` → `Service Providers`
  - `Maintenance` → `Maintenance Requests`
- تحسين layout في `client/components/pages/provider-portal.tsx`:
  - توحيد عرض التبويبات داخل container بعرض مناسب (`max-w-4xl`) مع padding متسق.
  - تغيير زر `Visit Report` إلى `New Report` عند الحاجة.
  - إضافة `History` tab مستقل لتقارير الزيارات مع فلترة حسب الوحدة + زر `View Photos`.
  - إضافة `View History` من بطاقات الوحدات.

**Why / Impact**
- تقليل الالتباس بين اسم التبويب ومحتواه الفعلي.
- جعل تجربة الصيانة قابلة للاستخدام من البداية للنهاية (اختيار اشتراك → إرسال طلب).
- إعطاء المضيف تحكم واضح بإلغاء الاشتراك.
- تحسين UX في بوابة المزود وإزالة عناصر “out of place”.
- تمكين المزود من مراجعة التقارير السابقة بدل إعادة إنشاء تقارير بدون رؤية التاريخ.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (توافق الواجهة بعد فصل صور التقارير)
**What**
- توثيق توافق الواجهة مع تغيير backend الخاص بفصل صور تقارير الزيارات إلى جدول مخصص.
- لا توجد تعديلات واجهة كبيرة جديدة في هذا التحديث؛ التركيز كان على توافق العقود والنتائج.

**Backend Contract Impact on Frontend**
- Endpoints الخاصة بتقارير الزيارات بقيت بنفس شكل الاستجابة (`photos` كروابط صور).
- لذلك صفحات المضيف/المزوّد التي تعرض صور التقارير لا تحتاج تغيير UI إضافي بعد هذا الترحيل.

**Why / Impact**
- استمرار عمل الواجهة بدون كسر بعد ترحيل قاعدة البيانات.
- تقليل المخاطر أثناء الانتقال التدريجي من النموذج القديم إلى النموذج المفصول.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (اشتراكات المضيف + فلاتر مهام المزوّد)
**What**
- إصلاح مشكلة ظهور طلبات/اشتراكات مكررة للمضيف لنفس المزوّد.
- تفعيل فلاتر أعلى تبويب المهام في بوابة المزوّد.

**How**
- تحديث `client/components/pages/settings-cleaning.tsx`:
  - إضافة `dedupedSubscriptions` عبر `useMemo`.
  - الدمج حسب `providerId` مع أولوية الحالة:
    - `ACCEPTED` ثم `PENDING` ثم `DECLINED`
  - استخدام القائمة المدمجة في عرض “My Subscriptions” وفي تحديد حالة المزوّد.
- تحديث `client/components/pages/provider-portal.tsx`:
  - إضافة فلاتر تشغيلية في قسم `All Assignments`:
    - `Pending`, `Open`, `Delivered`, `Accepted`, `Rejected`
  - ربطها بحالات DB الفعلية:
    - `Pending` = assignment `PENDING` أو task `PENDING`
    - `Open` = task `RESERVED | WORKING | APPROVAL_PENDING`
    - `Delivered` = task `COMPLETED`
    - `Accepted` = assignment `ACCEPTED`
    - `Rejected` = assignment `REJECTED` أو task `REJECTED | CANCELLED`
  - إضافة عدّاد لكل فلتر.

**Verification**
- `npm --prefix client run build` ✅

**Why / Impact**
- واجهة المضيف لم تعد تعرض حالات متضاربة لنفس المزوّد.
- فلاتر المزوّد أصبحت عملية وتعكس حالة المهام الحقيقية في النظام.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (تحسينات Provider Portal + توافق Admin Cleaning)
**What**
- تحسين تنظيم الشبكة والعرض في `Provider Portal` (Requests/Properties/History).
- تفعيل فلاتر Requests العلوية كفلاتر فعلية مع إضافة `All`.
- إزالة عنصر `View History` المكرر من تبويب Properties.
- إصلاح تموضع modal صور التقارير على الشاشات الكبيرة.
- توحيد عناصر select داخل Provider Portal لتفادي dropdown خارج الشاشة.

**How**
- `client/components/pages/provider-portal.tsx`:
  - Requests:
    - إضافة فلاتر حالة: `All`, `Pending`, `Accepted`, `Declined` مع العدّادات.
    - تطبيق فلترة حقيقية على الكروت حسب التبويب المختار.
    - تحسين grid لعرض properties داخل request card بشكل موحد.
  - Properties:
    - توحيد row layout بنظام grid ثابت (`content | actions`) مع `truncate` للأسماء الطويلة.
    - توحيد أحجام الصور والأزرار.
    - حذف زر `View History` من Properties (الاعتماد على تبويب History فقط).
  - History:
    - استخدام `Select` (UI component) بدل native select.
    - تعديل modal الصور ليكون داخل viewport:
      - `w-[min(96vw,980px)]`
      - `max-h-[88vh]`
      - `overflow-y-auto`
      - grid responsive للصور (`1/2/3` أعمدة حسب العرض).
  - Form الصور:
    - تحويل select category إلى `Select` component.
    - إصلاح تحديث صف الصورة الصحيح عند الفلترة باستخدام `actualIndex`.

**Verification**
- `npm --prefix client run build` ✅

**Why / Impact**
- واجهة أكثر اتساقاً وتماسكاً على الموبايل والديسكتوب.
- إزالة العناصر المكررة وتحسين قابلية التصفح.
- منع مشاكل overflow/dropdown positioning في modals/selects.
- تجربة أوضح للمزوّد أثناء إدارة الطلبات والتقارير.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Admin Cleaning: إصلاح عرض الصور)
**What**
- إصلاح مشكلة ظهور `0 photos` في صفحة `Admin > Cleaning > Visit Reports` رغم وجود صور حقيقية.
- إصلاح container الخاص بصور thumbnails داخل modal الصور.

**How**
- تحديث `client/components/pages/admin/admin-cleaning.tsx`:
  - إضافة `normalizePhotoUrls` لدعم أكثر من شكل لاستجابة الصور:
    - `photos: string[]`
    - `photoUrls: string[]`
    - مصفوفات objects مثل `{ url }` أو `{ imageUrl }`
  - استخدام التطبيع أثناء map لبيانات visit reports.
  - تعديل حاوية thumbnail لتكون `relative` عند استخدام `next/image` مع `fill`.

**Verification**
- بعد التحديث أصبحت بيانات التقارير تظهر في الصفحة مع قابلية فتح الصور عند توفرها من الـ API.

**Why / Impact**
- الواجهة أصبحت أكثر تحمّلًا لاختلافات contract البسيطة بين نسخ الـ backend.
- منع حالات “بيانات موجودة لكن الصور = 0” بسبب اختلاف شكل الحقل فقط.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (دفعة شاملة: Admin UX + Marketplace + Auth flows)
**What**
- تحويل `Admin > Marketplace` ليكون مركز الإدارة الأساسي لشركات Marketplace.
- نقل ميزات صفحة Providers القديمة إلى داخل Marketplace (Legacy tab) بدون حذف المكوّن القديم.
- إضافة Notifications bell للأدمن في الهيدر العلوي.
- إضافة Logout في أعلى اليمين لكل أنواع المستخدمين.
- توحيد سلوك تسجيل الخروج: كل Logout يرجع إلى Landing (`/`) بدل صفحات login.
- إصلاح مشاكل mobile overflow في صفحة Marketplace.
- إصلاح مشكلة `0 photos` في Admin Cleaning عندما تكون الصور موجودة فعليًا.

**How**
- Marketplace page:
  - إعادة بناء `client/components/pages/admin/admin-marketplace.tsx`:
    - تبويب `Marketplace Companies` لإدارة الشركات.
    - تبويب `Legacy Provider Tools` يعرض `AdminProviders` (المكوّن القديم) داخل نفس الصفحة.
    - إنشاء شركة + اختيار Company Admin + إسناد إداري للشركات الحالية.
    - فلاتر نوع الخدمة + عدادات.
  - إزالة عنصر `Providers` من sidebar الرئيسي للأدمن وتوحيد المسار داخل Marketplace:
    - `client/components/pages/admin/admin-layout.tsx`.
- Admin notifications:
  - إضافة bell + badge unread + dropdown + mark read/mark all read في هيدر الأدمن:
    - `client/components/pages/admin/admin-layout.tsx`.
- Logout UX:
  - Main app layout: إضافة زر Logout أعلى اليمين:
    - `client/components/layout/app-layout.tsx`.
  - Admin layout: إضافة زر Logout أعلى اليمين بجوار notifications.
  - Provider portal: جعل زر Logout العلوي ظاهرًا على desktop/mobile، وتوجيهه إلى `/` بعد logout:
    - `client/components/pages/provider-portal.tsx`.
  - صفحة verify-email: logout صار يرجع `/` وزر النص أصبح “Go home”:
    - `client/app/verify-email/page.tsx`.
- Mobile overflow fixes:
  - `TabsList` في marketplace أصبحت `overflow-x-auto`.
  - تغيير عرض select الإداري من ثابت إلى responsive (`w-full sm:w-[320px]`).
- Admin cleaning images fix:
  - إضافة normalization للصور في `client/components/pages/admin/admin-cleaning.tsx` لدعم صيغ متعددة:
    - `photos`, `photoUrls`, `{url}`, `{imageUrl}`.
  - إصلاح parent container لصور thumbnails مع `next/image fill` (`relative`).

**Verification**
- `npm --prefix client run build` ✅ (عدة مرات بعد كل دفعة تعديلات)

**Why / Impact**
- توحيد إدارة Marketplace في مكان واحد مع الاحتفاظ بالأدوات القديمة للتوافق.
- تحسين onboarding والتشغيل اليومي (notifications + logout موحد + واجهة mobile مستقرة).
- تقليل حالات فشل العرض الناتجة عن اختلاف contracts بين نسخ backend/frontend.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Provider Portal: Company Admin UI)
**What**
- إضافة واجهة Company Admin فعلية داخل `/provider` عند تسجيل دخول مستخدم مرتبط كشركة (`adminUserId` في الشركة).
- فصل العرض بين:
  - Provider worker/freelancer dashboard
  - Company Admin dashboard

**How**
- إنشاء صفحة جديدة:
  - `client/components/pages/provider-company-admin-dashboard.tsx`
- تبويبات Company Admin المفعلة:
  - `Dashboard`
  - `Free`
  - `Working`
  - `Clients`
  - `Requests`
  - `Company Data`
- ربط مباشر مع endpoints:
  - `GET /api/admin/provider-dashboard`
  - `GET /api/admin/employees`
  - `GET /api/admin/employees/free`
  - `GET /api/admin/employees/working`
  - `GET /api/admin/clients`
  - `GET /api/admin/subscription-requests`
  - `POST /api/admin/subscription-requests/:id/accept`
  - `POST /api/admin/subscription-requests/:id/reject`
  - `GET /api/admin/requests/pending-approval`
  - `POST /api/admin/requests/:id/approve`
  - `POST /api/admin/requests/:id/reject`
  - `GET /api/admin/company`
  - `PUT /api/admin/company`
- تحديث `client/components/pages/provider-portal.tsx`:
  - استخدام `isCompanyAdmin` من `auth-check`.
  - عرض Company Admin dashboard تلقائيًا إذا المستخدم Company Admin.

**Verification**
- `npm --prefix client run build` ✅

**Why / Impact**
- المستخدم المخصص كـ Company Admin يرى أدواته الإدارية المتوقعة بدل واجهة العامل العادي.
- تنفيذ فعلي لمتطلبات dashboard الإداري للشركة داخل provider portal.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Provider: Dual Mode Switch)
**What**
- إضافة التبديل بين وضعي المستخدم إذا كان يملك الصفتين:
  - `Provider Worker Mode`
  - `Company Admin Mode`

**How**
- تحديث `client/components/pages/provider-portal.tsx`:
  - إضافة state `mode` وتحديد الوضع تلقائيًا من نتيجة `auth-check`.
  - إذا كان المستخدم يحمل الصفتين (`isProvider` + `isCompanyAdmin`) يظهر زر تبديل الوضع.
- تحديث `client/components/pages/provider-company-admin-dashboard.tsx`:
  - إضافة زر الرجوع إلى `Worker Mode` عند توفر الصفتين.

**Verification**
- `npm --prefix client run build` ✅

**Why / Impact**
- نفس الحساب يمكنه إدارة الشركة وتنفيذ مهام العامل بدون تسجيل خروج أو حساب منفصل.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Logs Sync: Provider Dual Mode)
**What**
- توثيق تفعيل التبديل بين `Admin Mode` و`Worker Mode` داخل `/provider` للحسابات متعددة الصلاحية.

**How**
- `client/components/pages/provider-portal.tsx`:
  - state للتبديل بين الوضعين
  - أزرار تبديل مرئية عند امتلاك الصفتين
- `client/components/pages/provider-company-admin-dashboard.tsx`:
  - زر رجوع إلى `Worker Mode`

**Verification**
- `npm --prefix client run build` ✅

**Impact**
- استخدام حساب واحد لإدارة الشركة وتنفيذ مهام العامل بدون تسجيل خروج.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Company Admin UI: Invite Employee Modal)
**What**
- إضافة modal دعوة موظف داخل لوحة Company Admin في `/provider`.
- النموذج يطابق المطلوب: `First name`, `Last name`, `Email` فقط (بدون Plan).

**How**
- تحديث `client/components/pages/provider-company-admin-dashboard.tsx`:
  - زر `Invite Employee` في قسم الموظفين.
  - Dialog يتضمن 3 حقول required.
  - استدعاء `POST /api/admin/employees/invite`.
  - بعد النجاح: إغلاق modal + تحديث بيانات الموظفين + رسالة نجاح بأن الحساب pending verification.

**Verification**
- `npm --prefix client run build` ✅

**Impact**
- تدفق onboarding للموظفين صار كامل من داخل Company Admin portal.

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Employee Role Routing Fix)
**What**
- إصلاح التوجيه في الواجهة لحسابات `employee` بعد التحقق من البريد/الدخول.

**How**
- `client/app/provider/layout.tsx`:
  - السماح في RoleGuard بالأدوار `provider` و`employee`.
- `client/components/auth/role-guard.tsx`:
  - إعادة توجيه `employee` إلى `/provider` مثل provider.
- `client/components/pages/login.tsx`:
  - عند تسجيل دخول `employee` يتم التوجيه إلى `/provider`.
- `client/components/pages/signup.tsx`:
  - إذا كان المستخدم الحالي `employee` يتم توجيهه إلى `/provider`.
- `client/app/auth/verify-email/page.tsx`:
  - بعد OTP: `employee` يذهب إلى `/provider`.

**Verification**
- `npm --prefix client run build` ✅

**Impact**
- مسار onboarding للموظف المدعو أصبح مكتملًا: دعوة → إنشاء كلمة مرور → تحقق بريد → دخول إلى بوابة المزود.

---

## ✅ تحديث إغلاق اليوم (17 فبراير 2026)
- تم عكس منطق الحجب الجديد في الواجهة:
  - زر Block/Unblock مبني على `companyBlocked` وليس `status`.
  - Badge واضح للموظف المحجوب.
- تم تثبيت توجيه دور `employee` على جميع نقاط الدخول/التحقق إلى `/provider`.
- تم التحقق ببناء Next.js بنجاح بعد التعديلات.

---

## Session 17 — 17 فبراير 2026 (Portal Separation + UI Consistency + Provider Company Signup)
### What
- توحيد هيكل الـ Sidebar بين البوابات (Host/Provider/Provider Admin) ليتطابق مع نمط Admin.
- إصلاح عرض صورة المستخدم في قائمة مستخدمي Admin (`/admin/users`) بعد تحديث الصورة من Profile.
- منع انتقال الأدوار غير المضيف إلى إعدادات المضيف `/settings/*`.
- تطبيق فصل البوابات حسب الـ subdomain (admin/provider/hoster) تمهيداً لربط Nginx.
- إعادة تنظيم ملفات الصفحات إلى مجلدات أدوار واضحة (`host/`, `provider/`, `shared/`).
- إضافة صفحة تسجيل منفصلة لمشرف شركة المزود (multi-step) بدل التسجيل المختلط داخل provider login.

### How
- تحديث Sidebars في:
  - `client/components/layout/sidebar.tsx`
  - `client/components/pages/provider/provider-portal.tsx`
  - `client/components/pages/provider/provider-company-admin-dashboard.tsx`
- إضافة `AvatarImage` في Admin Users:
  - `client/components/pages/admin/admin-users.tsx`
- تقييد إعدادات المضيف عبر layout guard:
  - `client/app/settings/layout.tsx`
- فصل مسارات البوابات بالـ subdomain middleware:
  - `client/middleware.ts`
- إعادة تنظيم الملفات:
  - نقل صفحات المضيف إلى `client/components/pages/host/*`
  - نقل صفحات المزود إلى `client/components/pages/provider/*`
  - إضافة مكون مشترك `client/components/pages/shared/portal-settings-shortcuts.tsx`
- إضافة صفحة التسجيل المنفصلة:
  - `client/components/pages/provider/provider-company-signup.tsx`
  - `client/app/provider-signup/page.tsx`
- تعديل login الخاص بالمزود لعرض رابط صفحة التسجيل الجديدة:
  - `client/components/pages/provider/provider-portal.tsx`

### Why / Impact
- فصل منطقي وتشغيلي واضح بين البوابات استعداداً لبنية subdomains في الإنتاج.
- تجربة UI متسقة بين الأدوار.
- منع تسريب تجربة إعدادات المضيف إلى أدوار أخرى.
- تدفق تسجيل مزودي الشركات أصبح مطابقاً للمتطلبات (خطوات، تحقق بريد، انتظار موافقة).

---

## 🧾 تحديثات اليوم — 17 فبراير 2026 (Sync من `client/frontend_today.md`)
> ملاحظة: هذا القسم هو “Checklist Index” لكل ما تم توثيقه اليوم داخل `client/frontend_today.md` لضمان عدم ضياع أي تغيير بين الملفات.

### Portal separation + routing + Nginx docs
- (1) توحيد Sidebars عبر البوابات (Host/Provider/Provider Admin).
- (2) إصلاح صور المستخدمين داخل Admin Users.
- (3) استبدال `window.confirm` بمودال تأكيد.
- (4) منع أدوار غير المضيف من الوصول إلى `/settings/*`.
- (5) فصل البوابات حسب subdomain عبر `client/middleware.ts`.
- (6) إعادة تنظيم ملفات الصفحات حسب الأدوار.
- (7) استخراج مكوّنات مشتركة لصفحات البوابات.
- (8) صفحة تسجيل منفصلة لشركات المزودين (multi-step).
- (9) توثيق إعدادات Nginx للبوابات الأمامية.

### Chat UI unification
- (10) توحيد شاشة Inbox للمضيف (Guests / Providers / Support).
- (11) إضافة صفحة محادثة مخصصة للمزود داخل بوابة المضيف.
- (12) ربط تبويب Clients داخل Company Admin بشاشات الشات.
- (13) تنبيه صوتي للإشعارات الجديدة أثناء وجود المستخدم Online.
- (14) الحفاظ على توافق هيكل البوابات بعد تغييرات الشات.

### Support assignment (first accept wins)
- (15) ربط شاشات دعم Host/Provider بإنشاء طلب دعم رسمي.
- (16) تعديل زر Call Support في بوابة المزود لفتح الدعم الصحيح.
- (17) إضافة منطق Accept داخل Admin Chat UI (لا إرسال قبل التعيين).

### Theme + profile consistency + notifications UX + mobile chat composer
- (18) استخراج Theme utilities موحّدة (Spacing/Grid/Typography).
- (19) توحيد UI صفحات Profile (Host/Admin/Provider).
- (20) إضافة Back button لصفحة Provider Settings.
- (21) Deep linking للإشعارات (الضغط يفتح صفحة الإجراء الصحيح).
- (22) إدارة الإشعارات (Clear + حذف فردي X) من الواجهة.
- (23) تثبيت موضع chat composer فوق bottom nav على الموبايل (لكل صفحات الشات).
- (24) تحويل Company Data إلى صفحة إدارة كاملة داخل Company Admin.
- (25) إصلاح TypeScript include لتجنب الاعتماد على `.next/types`.

### Realtime sockets + chat polish
- (26) استبدال polling بـ WebSocket للإشعارات والشات.
- (27) Auto-scroll عند وصول رسالة جديدة (كل صفحات الشات).
- (28) تثبيت composer في Provider Support Chat.

### Provider auth separation + portal-aware auth + perf
- (29) فصل صفحات Login/Signup الخاصة بالمزوّد داخل مجلد البوابة + routes جديدة.
- (30) إرسال `portal` في كل طلبات auth لضمان تقييد الدخول حسب البوابة.
- (31) تحسين realtime/query invalidation لتقليل الضغط على API وتسريع الواجهة.

### Backend-only encryption update
- (32) لا تغييرات على الواجهة في دفعة تشفير الرسائل (Backend-only).

---

## 🧾 تحديثات اليوم — 18 فبراير 2026 (Chat Optimistic UI + Sender-First UX)
### Chat send latency fix across portals
- (33) إصلاح تأخير ظهور رسالة المرسل في `Admin Support Chat` عبر optimistic message + rollback + reconcile.
- (34) إصلاح تأخير شات الضيوف في بوابة المضيف (`/api/chat/:id/send`) مع حالة `sending` ثم `sent`.
- (35) تطبيق نفس السلوك في Support Chat للمضيف والمزوّد.
- (36) تطبيق optimistic updates في شات المزود داخل Inbox المضيف (مصدر cleaning + marketplace).
- (37) تطبيق optimistic updates في شات المزوّد داخل `Provider Portal` و`Company Admin Dashboard`.
- (38) تطبيق optimistic updates في شات التنظيف داخل `host/settings-cleaning`.

### Verification
- `npm --prefix client run build` ✅

### Impact
- الرسائل تظهر فوراً للمرسل في كل واجهات الشات بدون انتظار refetch، مع الحفاظ على تزامن نهائي مع السيرفر وRealtime.

---

## 🧾 تحديثات اليوم — 18 فبراير 2026 (Portal Clean URLs + Subdomain Isolation)
### Domain-first routing
- (39) تحويل التوجيه ليعتمد على subdomain أولاً، مع إبقاء landing على `staging.tryhoster.com`.
- (40) دعم روابط Admin النظيفة (`/login`, `/users`, `/settings`) على `staging.admin.tryhoster.com` عبر rewrite داخلي.
- (41) دعم روابط Provider النظيفة (`/login`, `/signup`, `/company-signup`, `/settings`) على `staging.provider.tryhoster.com` عبر rewrite داخلي.
- (42) دعم روابط Host النظيفة على `staging.hoster.tryhoster.com` مع تحويل `/` إلى `/dashboard`.
- (43) إعادة توجيه أي محاولة دخول لبوابة خاطئة إلى subdomain الصحيح بدل إبقائها داخل نفس النطاق.
- (44) تحديث `RoleGuard` لتوجيه غير المصرّح/اختلاف الدور إلى subdomain الصحيح لمنع حلقات redirect.
- (45) تحديث `app/admin/layout.tsx` و`app/provider/layout.tsx` لقبول public auth paths النظيفة.

### Verification
- `npm run build` ✅

### Impact
- URLs الإنتاج أصبحت domain-based بالكامل، مع عزل صارم بين بوابة Admin/Provider/Host وUX متسق في تسجيل الدخول.

---

## 🧾 تحديثات اليوم — 18 فبراير 2026 (Staging Redirect Host Fix)
### Subdomain host normalization
- (46) إصلاح تكوين hostname في التحويلات ليحافظ على `staging` prefix عند التنقل بين البوابات.
- (47) إزالة أي port من redirects (`:3000`) أثناء التوجيه بين subdomains.
- (48) توحيد بناء absolute portal URLs في `RoleGuard` لمنع التحويل إلى نطاقات غير متاحة.

### Verification
- `npm run build` ✅

### Impact
- انتهاء خطأ `ERR_CONNECTION_REFUSED` الناتج عن تحويلات `hoster.tryhoster.com:3000`.
