# Portal subdomain routing
PORTAL_HOST_SUBDOMAIN=hoster
PORTAL_PROVIDER_SUBDOMAIN=provider
PORTAL_ADMIN_SUBDOMAIN=admin

# Next.js server-side proxy target for /api and /uploads rewrites
API_PROXY_TARGET=http://127.0.0.1:5000

# Optional browser-side realtime/API overrides
# NEXT_PUBLIC_API_URL=https://api.example.com
# NEXT_PUBLIC_WS_URL=wss://api.example.com/ws
